apiVersion: v1
kind: Service
metadata:
  name: pdf-agent-service
  namespace: fsi
  labels:
    app: pdf-agent-api
spec:
  selector:
    app: pdf-agent-api
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
